version: '2'
services:
    web:
        build: .
        ports:
        - "80:5000"
        volumes:
            - .:/app
        command: flask run
    psql:
        image: postgres:latest
        environment:
            - POSTGRES_PASSWORD
    mongo:
        image: mongo:latest
